query getProductListing {
  getProductListing (published: false) {
    totalCount
    edges {
      node {
        name
        reference
        recommendedPriceTTC
        recommendedPromotionPriceTTC
        published
      }
    }
  }
}
